# DSM connection (fill these and save as .env)
SYNOLOGY_IP=192.0.2.25
SYNOLOGY_PORT=5001
SYNOLOGY_USERNAME=photos_sync
SYNOLOGY_PASSWORD=change_me

# Optional TOTP secret if DSM has 2FA enabled
SYNOLOGY_OTP_SECRET=

# Optional override for the JSON config path
SYNC_CONFIG_PATH=/volume1/homes/photos_sync/sync_config.json
